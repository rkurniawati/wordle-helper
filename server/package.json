{
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^5.0.0",
    "typescript": "^5.7.3"
  },
  "name": "wordle-server-ts",
  "version": "1.0.0",
  "description": "Wordle helper server in Typescript",
  "main": "index.js",
  "scripts": {
    "server": "npm run build && node dist/server.js",
    "build": "npx tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2"
  }
}
